// The event type definition
export type Event = {
  id: string;
  start: number; // minutes from 9:00 AM
  end: number; // minutes from 9:00 AM
};

export const layoutEvents = (events: Event[]) => {
  
};
